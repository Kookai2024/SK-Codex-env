/**
 * auth/types.ts
 * 기능 모듈에서 재사용할 타입 정의를 위한 플레이스홀더 파일입니다.
 *
 * 실제 구현 시 여기에 명확한 도메인 타입을 선언하고, 모든 필드는 주석으로 설명하세요.
 */

export interface AuthTypesPlaceholder {
  /** 이 플래그는 실제 타입 정의가 작성되기 전까지 모듈이 비어있지 않도록 도와줍니다. */
  placeholder: true;
}
